apiVersion: v1
kind: Service
metadata:
  name: go-web-app-service
  labels:
    app: go-web-app
spec:
  selector:
    app: go-web-app
  ports:
    - port: 80          # external port
      targetPort: 8080  # container port
      protocol: TCP
  type: LoadBalancer  

